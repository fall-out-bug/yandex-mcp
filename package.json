{
  "name": "yandex-mcp",
  "private": true,
  "version": "0.1.0",
  "description": "Per-user-credential MCP servers for Yandex services.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
